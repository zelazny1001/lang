    public static void appendStringToFile(String pathRoot, String port, String data) throws IOException {
        String filename = pathRoot + "_" + port;
        try (BufferedWriter writer = new BufferedWriter(new FileWriter(filename, true))) {
            writer.write(data);
            writer.newLine();
        }
    }