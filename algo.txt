     private static final Map<Byte, Integer> muLawDecodeMap = new HashMap<>();

    static {
        for (int i = 0; i <= 255; i++) {
            byte b = (byte) i;
            muLawDecodeMap.put(b, muLawDecode(b));
        }
    }
