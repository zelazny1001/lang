    public static float[] signed8bitToFloat(byte[] pcm) {
        float[] floatData = new float[pcm.length];
        for (int i = 0; i < pcm.length; i++) {
            floatData[i] = pcm[i] / 127.0f;
        }
        return floatData;
    }