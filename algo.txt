        int chunkSize = 160;
        int index = 0;

        while (index < floatArray.length) {
            float[] chunk = new float[chunkSize];
            System.arraycopy(floatArray, index, chunk, 0, chunkSize);
            processChunk(chunk);
            index += chunkSize;
        }