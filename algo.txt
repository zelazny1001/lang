    public boolean checkMostRecentKElements(int k) {
        if (values.size() < k) {
            return false;
        }
        for (int i = values.size() - k; i < values.size(); i++) {
            if (values.get(i) != 1) {
                return false;
            }
        }
        return true;
    }