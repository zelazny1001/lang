    public static boolean zerosAreAcceptable() {
        if (zeros.size() < ACCEPTABLE_CONSECUTIVE_ZEROS) {
            return false;
        }
        return zeros.subList(zeros.size() - ACCEPTABLE_CONSECUTIVE_ZEROS, zeros.size()).stream()
                .allMatch(val -> val == 1);
    }