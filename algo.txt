public class RangeToBoundaries {
    public static List<Integer> toPair(String input) {
        return Arrays.stream(input.split("-"))
                .map(Integer::parseInt)
                .collect(Collectors.toList());
    }
}
===========================================================

